apiVersion: apps/v1
kind: Deployment
metadata:
  name: oscen
  labels:
    app: oscen
    component: oscen
    environment: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oscen
      component: oscen
      environment: production
  template:
    metadata:
      labels:
        app: oscen
        component: oscen
        environment: production
    spec:
      containers:
        - name: oscen
          image: ghcr.io/oscenbot/oscen:d2cc7f2548c8954d334cc7bcb1d281434d1c2335
          ports:
            - containerPort: 9000
          envFrom:
            - secretRef:
                name: oscen
          env:
            - name: CALLBACK_HOST
              value: https://oscen.production.svc.oscen.io
