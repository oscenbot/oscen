apiVersion: apps/v1
kind: Deployment
metadata:
  name: oscen
  labels:
    app: oscen
    component: oscen
    environment: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oscen
      component: oscen
      environment: production
  template:
    metadata:
      labels:
        app: oscen
        component: oscen
        environment: production
    spec:
      containers:
        - name: oscen
          image: ghcr.io/oscenbot/oscen:2aa683904c918a343bd7b46bbccf3f25e6dd28cf
          ports:
            - containerPort: 9000
          envFrom:
            - secretRef:
                name: oscen
          env:
            - name: CALLBACK_HOST
              value: https://oscen.production.svc.oscen.io
            - name: JAEGER_URL
              value: http://main-collector.observability.svc.cluster.local:14268/api/traces
